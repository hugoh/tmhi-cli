# DO NOT EDIT — managed by hugoh/go-tools via copier.
version: 2

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
      - freebsd
    goarch:
      - amd64
      - arm64
      - "386"
      - arm
    goarm:
      - "6"
      - "7"
    ignore:
      - goos: darwin
        goarch: "386"
      - goos: darwin
        goarch: arm
      - goos: freebsd
        goarch: arm
      - goos: freebsd
        goarch: "386"
    flags:
      - -trimpath
      - -mod=readonly
    ldflags:
      - -s -w
      - -X main.version={{.Version}}

nfpms:
  - maintainer: "Hugo Haas <hugoh@hugoh.net>"
    description: "CLI for managing T-Mobile Home Internet gateway and signal monitoring"
    homepage: "https://github.com/hugoh/tmhi-cli"
    license: "MIT"
    formats:
      - deb
      - rpm
    bindir: /usr/bin

brews:
  # DEPRECATED but intentional: homebrew_casks requires Gatekeeper-signed
  # binaries or macOS flags the cask as malware. brews (Formula) has no
  # such requirement. See https://goreleaser.com/deprecations/#brews
  - name: tmhi-cli
    repository:
      owner: hugoh
      name: homebrew-tap
      token: "{{ .Env.TAP_GITHUB_TOKEN }}"
    directory: Formula
    description: "CLI for managing T-Mobile Home Internet gateway and signal monitoring"
    homepage: "https://github.com/hugoh/tmhi-cli"
changelog:
  use: git
  sort: asc
  groups:
    - title: "⚠️ Breaking Changes"
      regexp: '^.*?(\w+)(\(.+\))??!:.+$'
      order: 0
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: Bug Fixes
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 2
    - title: Documentation
      regexp: '^.*?docs?(\([[:word:]]+\))??!?:.+$'
      order: 3
    - title: Dependency Updates
      regexp: '^.*?(feat|fix|chore)\(deps\).*:.+$'
      order: 900
    - title: Others
      order: 999
  filters:
    exclude:
      - "^chore\\(release\\): prepare for"
      - "^chore\\(deps"
      - "Merge pull request"
      - "Merge branch"

checksum:

snapshot:
