version: "2"
linters:
  default: all
  disable:
    - depguard
    - exhaustruct
    - noinlineerr
    - paralleltest
    - testpackage
    - wsl
    - wsl_v5
  exclusions:
    rules:
      - path: _test\.go$
        linters:
          - err113
          - varnamelen
formatters:
  enable:
    - gci
    - gofumpt
    - goimports
    - golines
